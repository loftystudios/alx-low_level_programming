#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0
 */

int main(void)
{
	char lowerc = 'a';
	char upperc = 'A';

	for (lowerc == 'a'; 'a' <= 'z'; lowerc++)
	{
		putchar(lowerc);
	}
	while (upperc == 'A'; 'A' <= 'Z'; upperc++)
	{
		putchar(upperc);
	}
	putchar('\n')
	
	return (0);
}
